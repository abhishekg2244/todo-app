apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: get-task-ingress
  namespace: dev-ns
  
spec:
  ingressClassName: nginx
  rules:
  - host: get-task.ab-learn.xyz
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: get-task
            port: 
              number: 8000
