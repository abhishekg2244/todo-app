apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: delete-task-ingress
  namespace: dev-ns
  
spec:
  ingressClassName: nginx
  rules:
  - host: delete-task.ab-learn.xyz
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: delete-task
            port: 
              number: 8000
